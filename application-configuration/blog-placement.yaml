apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: blog
  namespace: wordpress
spec:
  clusterConditions:
   - type: OK
  clusterSelector:
    matchExpressions: []
    matchLabels:
      cloud: "Amazon"
  clusterReplicas: 1
