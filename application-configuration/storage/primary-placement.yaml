apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: primary
  namespace: rook-ceph 
spec:
  clusterSelector:
    matchLabels:
      cloud: "Amazon"
  clusterReplicas: 2
