apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: standby
  namespace: rook-ceph
spec:
  clusterConditions:
  - status: "True"
    type: ManagedClusterConditionAvailable
  clusterSelector:
    matchExpressions:
    - {key: environment, operator: In, values: ["east2"]}

